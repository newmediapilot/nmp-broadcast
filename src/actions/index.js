// Export all functions from this file
const getFileFromImages = require('./getFileFromImages');
const handlePromises = require('./handlePromises');
const completionMessage = require('./completionMessage');

module.exports = {
    getFileFromImages,
    handlePromises,
    completionMessage
};
